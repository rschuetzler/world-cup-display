// Copy to secrets.h and fill in. secrets.h is gitignored.
//
// Unlike the server-driven firmware there is no MATRIX_URL — this build talks
// to ESPN directly. Only Wi-Fi credentials are needed.
#pragma once

#define WIFI_SSID "your-ssid"
#define WIFI_PASSWORD "your-password"
